schema: api.graphql
documents: ./pages/_lib/graphql/**/*.graphql
generates:
  ./pages/_lib/graphql/sdk.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-graphql-request
config:
  documentMode: documentNode
